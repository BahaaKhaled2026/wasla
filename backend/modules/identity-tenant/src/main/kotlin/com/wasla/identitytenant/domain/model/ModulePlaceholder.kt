package com.wasla.identitytenant.domain.model

/**
 * Placeholder marker confirming the identity-tenant module compiles as part of
 * the multi-project build. Replace with real domain model types when VS-01
 * (tenant creation, activation, and branding) implementation begins.
 */
internal object ModulePlaceholder
